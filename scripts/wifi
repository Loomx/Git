#!/bin/sh
#
# /usr/local/bin/wifi
#
# Add '$USER ALL = NOPASSWD: /usr/bin/nm-applet, /bin/killall' to /etc/sudoers

sudo nm-applet 2>/dev/null &
stalonetray --geometry=2x1-0-0 2>/dev/null
sudo killall nm-applet
